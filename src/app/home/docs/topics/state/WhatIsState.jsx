import React from "react";

export default function WhatIsState() {
  return (
    <>
      <h1>what is state</h1>
      <ul data-ul="what is state">
        <li>variables can't update to component ui data so in that condition use state</li>
        <li>state is a object inwhich store to data like variables</li>
        <li>when we some update in state so component detect changes then component is re-render and update to data</li>
      </ul>
    </>
  );
}
